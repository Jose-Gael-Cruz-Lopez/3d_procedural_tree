
  # Procedural Tree V2 (Copy)

  This is a code bundle for Procedural Tree V2 (Copy). The original project is available at https://www.figma.com/design/xpnOfhu4wfblOGOEC1AHAC/Procedural-Tree-V2--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  